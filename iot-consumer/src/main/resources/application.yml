# GENERATED by inni microservice template v3.3.5
info:
  name: iot-application
  description: Iot device data collection service

server:
  port: ${PORT:8012}

spring:
  application:
    name: iot-application
  data:
    mongodb.host: localhost
    mongodb.port: 32769
    mongodb.uri: mongodb://localhost/test
    mongo.repositories.enabled: true

kafka:
  consumer:
    bootstrap: localhost:9092
    group: iotDeviceGroup
    topic: iotdevice